const friend = (listOfFriends) => {
  const result = listOfFriends.filter(friend => friend.length === 4)
  return result
}

// const friend = (listOfFriends) => listOfFriends.filter(friend => friend.length === 4)


friend(["Jimm", "Cari", "aret", "truehdnviegkwgvke", "sixtyiscooooool"] )

